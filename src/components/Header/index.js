import React from 'react';
import styles from './styles';

const Header = () => {
  return (
    <div style={styles.headerContainer}>
      <p style={styles.headerTitle}>
        <strong>RandomDisco</strong> presents
      </p>
    </div>
  )
};

export default Header;
